# Loaded in every mode (dev and production build). Must match the backend's
# API_SHARED_TOKEN. Not a real secret — see src/config.ts for why.
VITE_API_TOKEN=bc6295965eef2054b424afecdbfa1eee82a933bd7d21786e
